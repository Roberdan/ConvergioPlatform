# Cross-Repo Migration: Script References Audit

> Generated by plan 685, task T5-01 (F-19). Audited 2026-03-22.

This document catalogs all references to legacy Convergio shell scripts across
satellite repos. Each entry includes the file, line number, the current
reference, and the suggested `cvg` CLI replacement.

## Script Replacement Map

| Legacy Script | `cvg` Replacement |
|---|---|
| `plan-db.sh <cmd>` | `cvg <cmd>` |
| `plan-db-safe.sh update-task <id> done` | `cvg task done <id>` |
| `planner-create.sh create` | `cvg plan create` |
| `planner-create.sh import` | `cvg plan import` |
| `wave-worktree.sh create` | `cvg worktree create` |
| `wave-worktree.sh merge` | `cvg worktree merge` |
| `wave-worktree.sh merge-async` | `cvg worktree merge --async` |
| `wave-worktree.sh pr-sync` | `cvg worktree pr-sync` |
| `plan-checkpoint.sh save` | `cvg checkpoint save` |
| `plan-checkpoint.sh restore` | `cvg checkpoint restore` |
| `file-lock.sh acquire` | `cvg lock acquire` |
| `file-lock.sh release` | `cvg lock release` |

---

## Repo: MirrorBuddy

**Path**: `/Users/Roberdan/GitHub/MirrorBuddy`
**Total references**: 16 in 5 files

### `.github/agents/planner.agent.md`

| Line | Current Reference | Suggested Replacement |
|------|------------------|-----------------------|
| 3 | `Uses plan-db.sh.` | `Uses cvg (Convergio CLI).` |

### `.github/copilot-instructions.md`

| Line | Current Reference | Suggested Replacement |
|------|------------------|-----------------------|
| 67 | `` `plan-db-safe.sh update-task {id} done` `` | `` `cvg task done {id}` `` |

### `AGENTS.md`

| Line | Current Reference | Suggested Replacement |
|------|------------------|-----------------------|
| 27 | `plan-db-safe.sh update-task {id} done` | `cvg task done {id}` |

### `docs/adr/0116-documentation-ai-ready-architecture.md`

| Line | Current Reference | Suggested Replacement |
|------|------------------|-----------------------|
| 198 | `` `plan-db.sh validate {id}` `` | `` `cvg validate-wave {id}` `` |

### `docs/claude/copilot-integration.md`

| Line | Current Reference | Suggested Replacement |
|------|------------------|-----------------------|
| 14 | `` `plan-db.sh` commands (same SQLite DB) `` | `` `cvg` commands (shared plan DB) `` |
| 29 | `` `plan-db.sh kanban` in terminal `` | `` `cvg kanban` in terminal `` |
| 44 | `plan-db.sh list 1` | `cvg plan list 1` |
| 56 | `plan-db.sh list-tasks {plan_id}` | `cvg task list {plan_id}` |
| 59 | `plan-db.sh get-task {task_id}` | `cvg task get {task_id}` |
| 66 | `plan-db.sh update-task {task_id} in_progress "Started"` | `cvg task update {task_id} in_progress "Started"` |
| 75 | `plan-db.sh update-task {task_id} done "Summary"` | `cvg task done {task_id} "Summary"` |
| 87 | `plan-db.sh validate {plan_id}` | `cvg validate-wave {plan_id}` |
| 120 | `` `plan-db.sh` — Plan/task/wave management `` | `` `cvg` — Plan/task/wave management `` |
| 201 | `plan-db.sh list-tasks {plan_id}` | `cvg task list {plan_id}` |
| 205 | `plan-db.sh get-task {task_db_id}` | `cvg task get {task_db_id}` |
| 208 | `plan-db.sh update-task {task_db_id} done 'Summary'` | `cvg task done {task_db_id} 'Summary'` |

**Summary**: All references are documentation-only (no executable scripts). Update
docs to point to `cvg` CLI. No runtime changes needed in MirrorBuddy.

---

## Repo: VirtualBPM

**Path**: `/Users/Roberdan/GitHub/VirtualBPM`
**Total references**: 2 in 1 file

### `.claude/plans/review-challenger.md`

| Line | Current Reference | Suggested Replacement |
|------|------------------|-----------------------|
| 83 | `wave-worktree.sh merge` (describes cleanup behavior) | `cvg worktree merge` |
| 88 | `wave-worktree.sh create <new_plan_id> <wave_db_id>` | `cvg worktree create <new_plan_id> <wave_db_id>` |

**Summary**: Single plan review doc with two `wave-worktree.sh` references. Both
are in instructional text, not executable code. Update to `cvg worktree`.

---

## Repo: MaranelloLuceDesign

**Path**: `/Users/Roberdan/GitHub/MaranelloLuceDesign`
**Total references**: 38 in 5 files

### `.github/agents/planner.agent.md`

| Line | Current Reference | Suggested Replacement |
|------|------------------|-----------------------|
| 3 | `Uses plan-db.sh as single source of truth.` | `Uses cvg as single source of truth.` |
| 137 | `plan-db.sh conflict-check-spec $PROJECT_ID ...` | `cvg conflict-check-spec $PROJECT_ID ...` |
| 147 | `plan-db.sh create $PROJECT_ID "{PlanName}" --source-file "$PROMPT_FILE" --auto-worktree` | `cvg plan create $PROJECT_ID "{PlanName}" --source-file "$PROMPT_FILE" --auto-worktree` |
| 148 | `plan-db.sh import $PLAN_ID /path/to/spec.json` | `cvg plan import $PLAN_ID /path/to/spec.json` |
| 149 | `plan-db.sh get-worktree $PLAN_ID` | `cvg plan get-worktree $PLAN_ID` |
| 155 | `plan-db.sh json $PLAN_ID` | `cvg plan json $PLAN_ID` |
| 173 | `plan-db.sh start $PLAN_ID` | `cvg plan start $PLAN_ID` |
| 180 | `plan-db.sh create <proj> <name> --source-file <path> --auto-worktree` | `cvg plan create <proj> <name> --source-file <path> --auto-worktree` |
| 181 | `plan-db.sh import <plan_id> <spec.json>` | `cvg plan import <plan_id> <spec.json>` |
| 182 | `plan-db.sh start <plan_id>` | `cvg plan start <plan_id>` |
| 183 | `plan-db.sh drift-check <plan_id>` | `cvg drift-check <plan_id>` |
| 184 | `plan-db.sh get-context <plan_id>` | `cvg plan get-context <plan_id>` |

### `.github/agents/execute.agent.md`

| Line | Current Reference | Suggested Replacement |
|------|------------------|-----------------------|
| 22 | `plan-db.sh validate-wave` | `cvg validate-wave` |
| 44 | `plan-db-safe.sh sets 'submitted'` | `cvg task done` sets 'submitted' |
| 56 | `plan-db.sh list "$PROJECT_ID"` | `cvg plan list "$PROJECT_ID"` |
| 57 | `plan-db.sh get-context $PLAN_ID` | `cvg plan get-context $PLAN_ID` |
| 62 | `plan-db.sh auto-approve $PLAN_ID ...` | `cvg plan auto-approve $PLAN_ID ...` |
| 69 | `wave-worktree.sh create $PLAN_ID $FIRST_WAVE_ID` | `cvg worktree create $PLAN_ID $FIRST_WAVE_ID` |
| 70 | `plan-db.sh get-context $PLAN_ID` | `cvg plan get-context $PLAN_ID` |
| 76 | `plan-db.sh set-worktree $PLAN_ID "$WORKTREE_PATH"` | `cvg plan set-worktree $PLAN_ID "$WORKTREE_PATH"` |
| 81 | `plan-db.sh start $PLAN_ID` | `cvg plan start $PLAN_ID` |
| 82 | `plan-db.sh check-readiness $PLAN_ID` | `cvg plan check-readiness $PLAN_ID` |
| 88 | `plan-db.sh drift-check $PLAN_ID` | `cvg drift-check $PLAN_ID` |
| 94 | `plan-db.sh rebase-plan $PLAN_ID` | `cvg plan rebase $PLAN_ID` |
| 110 | `plan-db.sh update-task {db_task_id} in_progress "Started"` | `cvg task update {db_task_id} in_progress "Started"` |
| 144 | `plan-db-safe.sh update-task {db_task_id} done "Summary ..."` | `cvg task done {db_task_id} "Summary ..."` |
| 155 | `plan-db-safe.sh update-task {db_task_id} done "Summary" \` | `cvg task done {db_task_id} "Summary" \` |
| 167 | `plan-db.sh validate-wave $WAVE_DB_ID` | `cvg validate-wave $WAVE_DB_ID` |
| 175 | `wave-worktree.sh merge-async $PLAN_ID $WAVE_DB_ID` | `cvg worktree merge --async $PLAN_ID $WAVE_DB_ID` |
| 176 | `wave-worktree.sh create $PLAN_ID $NEXT_WAVE_DB_ID` | `cvg worktree create $PLAN_ID $NEXT_WAVE_DB_ID` |
| 178 | `wave-worktree.sh pr-sync $PLAN_ID $NEXT_WAVE_DB_ID` | `cvg worktree pr-sync $PLAN_ID $NEXT_WAVE_DB_ID` |

### `.github/agents/validate.agent.md`

| Line | Current Reference | Suggested Replacement |
|------|------------------|-----------------------|
| 38 | `plan-db.sh list "$(echo "$INIT" | jq -r '.project_id')"` | `cvg plan list "$(echo "$INIT" | jq -r '.project_id')"` |
| 40 | `plan-db.sh get-worktree $PLAN_ID` | `cvg plan get-worktree $PLAN_ID` |
| 79 | `` `plan-db.sh check-readiness {plan}` `` | `` `cvg plan check-readiness {plan}` `` |
| 114 | `plan-db.sh validate-wave {wave_db_id}` | `cvg validate-wave {wave_db_id}` |
| 118 | `plan-db.sh update-task {task_db_id} in_progress "Thor rejection: reason"` | `cvg task update {task_db_id} in_progress "Thor rejection: reason"` |

### `plans/convergio-evolution-engine/spec.yaml`

| Line | Current Reference | Suggested Replacement |
|------|------------------|-----------------------|
| 597 | `plan-db.sh agent data and mesh heartbeat` | `cvg agent data and mesh heartbeat` |

### `plans/convergio-evolution-engine/spec-v2-archived.yaml`

| Line | Current Reference | Suggested Replacement |
|------|------------------|-----------------------|
| 497 | `plan-db.sh agent data and mesh heartbeat` | `cvg agent data and mesh heartbeat` |

**Summary**: All references are in Copilot GitHub agent configs (`.github/agents/`)
and plan spec files. These are the highest-priority files to update since Copilot
reads them at runtime to issue commands. The `.github/agents/execute.agent.md`
has the most references (19) and drives actual plan execution.

---

## Repo: MyConvergio

**Path**: `/Users/Roberdan/GitHub/MyConvergio`
**Total references**: 210+ files

MyConvergio is the **source repo** for the Convergio platform scripts. It contains
the actual implementations of `plan-db.sh`, `plan-db-safe.sh`, `wave-worktree.sh`,
`plan-checkpoint.sh`, and `file-lock.sh`. References here fall into two categories:

### Category A — Implementation Files (not to be migrated by script-rename alone)

These files *are* the scripts or contain the script implementations. They will be
superseded by the Rust `cvg` CLI as part of plan 685 (F-01 through F-18):

| Path | Status |
|------|--------|
| `scripts/mesh/plan-db.sh` | Source — superseded by `cvg` Rust binary |
| `scripts/mesh/plan-db-safe.sh` | Source — superseded by `cvg task done` |
| `scripts/mesh/wave-worktree.sh` | Source — superseded by `cvg worktree` |
| `scripts/mesh/plan-checkpoint.sh` | Source — superseded by `cvg checkpoint` |
| `scripts/mesh/file-lock.sh` | Source — superseded by `cvg lock` |
| `.claude/scripts/plan-db.sh` | Mesh copy — same as above |
| `.claude/scripts/plan-db-safe.sh` | Mesh copy — same as above |
| `.claude/scripts/wave-worktree.sh` | Mesh copy — same as above |
| `.claude/scripts/plan-checkpoint.sh` | Mesh copy — same as above |
| `.claude/scripts/file-lock.sh` | Mesh copy — same as above |

### Category B — Documentation + Agent Config Files (update to `cvg`)

Key documentation files with inline references (representative subset):

| File | References | Priority |
|------|-----------|----------|
| `copilot-config/copilot-instructions.md` | plan-db.sh (many) | HIGH — Copilot reads at runtime |
| `copilot-instructions.md` | plan-db.sh (many) | HIGH — Copilot reads at runtime |
| `.github/copilot-instructions.md` | plan-db.sh (many) | HIGH — Copilot reads at runtime |
| `CLAUDE.md` | plan-db.sh, plan-db-safe.sh | HIGH — Claude reads at session start |
| `.claude/rules/workflow-enforced.md` | plan-db-safe.sh | HIGH — enforced by hooks |
| `.claude/rules/guardian.md` | plan-db.sh | MEDIUM — reference |
| `.claude/agents/technical_development/task-executor.md` | plan-db-safe.sh | HIGH — agent prompt |
| `.claude/agents/core_utility/thor-quality-assurance-guardian.md` | plan-db.sh | HIGH — agent prompt |
| `docs/adr/*.md` (multiple) | plan-db.sh, wave-worktree.sh | LOW — historical ADRs, keep as-is |
| `.claude/hooks/*.sh` | plan-db.sh | HIGH — executed by hooks |

**Summary**: MyConvergio requires a two-phase approach. Phase 1: delete/archive
the shell scripts once the `cvg` binary is verified (plan 685 F-01–F-18). Phase 2:
update all documentation and agent config files to reference `cvg` instead.
Hook files that call `plan-db.sh` directly must be updated to call `cvg` in the
same PR as the script deletion (plan 685 T5-04 verification gate).

---

## Summary Table

| Repo | Files Affected | References | Priority | Action |
|------|---------------|-----------|----------|--------|
| MirrorBuddy | 5 | 16 | LOW | Docs-only; update terminology to `cvg` |
| VirtualBPM | 1 | 2 | LOW | Plan doc; update 2 lines to `cvg worktree` |
| MaranelloLuceDesign | 5 | 38 | HIGH | Agent configs drive live Copilot; update to `cvg` |
| MyConvergio | 210+ | 1000+ | CRITICAL | Source repo; delete scripts, update hooks + docs |

## Migration Order

1. **MyConvergio hooks** — update `plan-db.sh` calls in `hooks/` to `cvg` (blocking gate for T5-04)
2. **MyConvergio agent configs** — update `.claude/agents/` and `CLAUDE.md` to reference `cvg`
3. **MaranelloLuceDesign `.github/agents/`** — update Copilot agent configs to `cvg`
4. **MirrorBuddy docs** — low-urgency; update on next planned maintenance
5. **VirtualBPM** — single plan doc; update opportunistically

## Notes

- `planner-create.sh` was NOT found in any satellite repo. It exists only in the
  ConvergioPlatform main repo and MyConvergio (as part of the script ecosystem).
- `file-lock.sh` references in MaranelloLuceDesign and MirrorBuddy: **none found**.
  Only MyConvergio contains `file-lock.sh` implementations.
- `plan-checkpoint.sh` references in satellite repos: **none found** (only MyConvergio).
- All satellite repo references use `plan-db.sh` and/or `wave-worktree.sh`;
  the other scripts are internal implementation details not yet surfaced externally.
